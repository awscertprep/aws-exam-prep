---
const {
  title = 'AWS Exam Prep',
  description = 'Practice AWS certification scenarios with focused multiple-choice drills.',
  currentExam = 'sap-c02',
  repoUrl = 'https://github.com/awscertprep/aws-exam-prep.git',
  supportUrl = 'https://ko-fi.com/awscertprep',
  showBack = true,
  showLaunch = true,
} = Astro.props;

const exams = [
  {
    id: 'sap-c02',
    label: 'SA Pro',
    code: 'SAP-C02',
    base: '/aws/sap-c02',
    topics: [
      { label: 'High Availability', href: '/aws/sap-c02/high-availability' },
      { label: 'Networking', href: '/aws/sap-c02/networking' },
      { label: 'Security', href: '/aws/sap-c02/security' },
    ],
  },
  {
    id: 'dop-c02',
    label: 'DevOps Pro',
    code: 'DOP-C02',
    base: '/aws/dop-c02',
    topics: [
      { label: 'Pipelines', href: '/aws/dop-c02/pipelines' },
      { label: 'Observability', href: '/aws/dop-c02/observability' },
    ],
  },
];

const navLinks = [
  { label: 'Services', href: '/services' },
  { label: 'Interview Prep', href: '/interview-prep' },
  { label: 'Blog', href: '/blog' },
];

---

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{title}</title>
    <meta name="description" content={description} />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossOrigin="" />
    <link
      href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@400;500;600;700&family=Inter:wght@400;500;600&display=swap"
      rel="stylesheet"
    />
    <style>
      :root {
        --bg: #05060a;
        --panel: #0f1118;
        --card: #111423;
        --border: #1e2535;
        --muted: #c5ccd9;
        --text: #f5f7fb;
        --accent: #6decb9;
        --accent-2: #82b4ff;
        --shadow: 0 20px 80px rgba(0, 0, 0, 0.35);
      }
      * {
        box-sizing: border-box;
      }
      body {
        margin: 0;
        font-family: 'Space Grotesk', 'Inter', system-ui, -apple-system, sans-serif;
        background: radial-gradient(100% 100% at 0% 0%, #0b1021 0%, #05060a 48%),
          linear-gradient(145deg, rgba(109, 236, 185, 0.08), rgba(130, 180, 255, 0.08));
        color: var(--text);
        min-height: 100vh;
      }
      a,
      a:visited,
      a:active {
        color: var(--accent);
        text-decoration: none;
      }
      a:hover,
      a:focus-visible {
        color: var(--accent-2);
      }
      .bg-grid {
        position: fixed;
        inset: 0;
        background-image: linear-gradient(rgba(255, 255, 255, 0.02) 1px, transparent 1px),
          linear-gradient(90deg, rgba(255, 255, 255, 0.02) 1px, transparent 1px);
        background-size: 22px 22px;
        opacity: 0.5;
        pointer-events: none;
        z-index: 0;
      }
      header {
        position: sticky;
        top: 0;
        z-index: 2;
        backdrop-filter: blur(18px);
        background: rgba(5, 6, 10, 0.8);
        border-bottom: 1px solid var(--border);
      }
      .marquee {
        overflow: hidden;
        border-radius: 14px;
        border: 1px solid rgba(109, 236, 185, 0.5);
        background: linear-gradient(120deg, rgba(109, 236, 185, 0.08), rgba(130, 180, 255, 0.08));
        margin: 12px auto 0;
        box-shadow: var(--shadow);
        padding: 10px 0;
        max-width: 1100px;
      }
      .marquee-track {
        display: inline-flex;
        gap: 48px;
        white-space: nowrap;
        animation: marquee 24s linear infinite;
        will-change: transform;
      }
      .marquee span {
        color: var(--text);
        font-weight: 700;
        font-size: 13px;
        letter-spacing: 0.02em;
      }
      @keyframes marquee {
        0% {
          transform: translateX(0);
        }
        100% {
          transform: translateX(-50%);
        }
      }
      nav {
        max-width: 1100px;
        margin: 0 auto;
        padding: 18px 22px;
        display: flex;
        flex-direction: column;
        gap: 10px;
      }
      .nav-left {
        display: inline-flex;
        align-items: center;
        gap: 10px;
        flex-wrap: wrap;
      }
      .back-btn {
        border: 1px solid var(--border);
        background: rgba(255, 255, 255, 0.03);
        color: var(--text);
        padding: 8px 12px;
        border-radius: 10px;
        font-weight: 600;
        cursor: pointer;
        transition: border 0.2s ease, transform 0.1s ease;
      }
      .back-btn:hover {
        border-color: var(--accent);
        transform: translateY(-1px);
      }
      .brand {
        font-weight: 700;
        letter-spacing: -0.02em;
        display: inline-flex;
        align-items: center;
        gap: 8px;
        font-size: 20px;
      }
      .brand .pill {
        background: linear-gradient(120deg, rgba(109, 236, 185, 0.85), rgba(130, 180, 255, 0.85));
        color: #03060e;
        padding: 8px 12px;
        border-radius: 12px;
        font-size: 13px;
        font-weight: 800;
        letter-spacing: 0.04em;
      }
      .exam-bar {
        display: flex;
        justify-content: space-between;
        align-items: center;
        gap: 12px;
        flex-wrap: wrap;
      }
      .exam-menu {
        position: relative;
      }
      .exam-links {
        display: inline-flex;
        gap: 8px;
        align-items: center;
        flex-wrap: wrap;
      }
      .exam-links a {
        border: 1px solid var(--border);
        background: rgba(255, 255, 255, 0.03);
        padding: 8px 10px;
        border-radius: 10px;
        font-weight: 600;
        font-size: 14px;
        transition: border 0.2s ease, transform 0.1s ease;
      }
      .exam-links a:hover {
        border-color: var(--accent);
        transform: translateY(-1px);
      }
      .exam-trigger {
        border: 1px solid var(--border);
        background: rgba(255, 255, 255, 0.02);
        color: var(--accent);
        padding: 10px 14px;
        border-radius: 12px;
        font-weight: 700;
        font-size: 14px;
        letter-spacing: -0.01em;
        cursor: pointer;
        display: inline-flex;
        align-items: center;
        gap: 6px;
        transition: border 0.2s ease, transform 0.1s ease, background 0.2s ease;
      }
      .exam-trigger:hover,
      .exam-trigger:focus-visible {
        border-color: var(--accent);
        background: rgba(109, 236, 185, 0.08);
        transform: translateY(-1px);
        outline: none;
      }
      .exam-dropdown {
        position: absolute;
        right: 0;
        margin-top: 8px;
        min-width: 220px;
        background: var(--panel);
        border: 1px solid var(--border);
        border-radius: 14px;
        box-shadow: var(--shadow);
        padding: 8px;
        display: none;
        z-index: 5;
      }
      .exam-dropdown a {
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: 10px;
        border-radius: 10px;
        color: var(--text);
        transition: background 0.2s ease, border 0.2s ease;
        border: 1px solid transparent;
      }
      .exam-dropdown a:hover {
        background: rgba(109, 236, 185, 0.08);
        border-color: rgba(109, 236, 185, 0.6);
      }
      .exam-dropdown .code {
        color: var(--muted);
        font-size: 12px;
        letter-spacing: 0.04em;
      }
      main {
        position: relative;
        z-index: 1;
        max-width: 1100px;
        margin: 0 auto;
        padding: 32px 22px 80px;
      }
      .ad-container {
        width: 100%;
        display: flex;
        justify-content: center;
        margin: 16px 0 12px;
      }
      .ad-placeholder {
        width: min(100%, 728px);
        min-height: 90px;
        border: 1px dashed var(--border);
        border-radius: 12px;
        color: var(--muted);
        display: grid;
        place-items: center;
        background: rgba(255, 255, 255, 0.02);
        font-size: 13px;
        text-align: center;
        padding: 12px;
      }
      .surface {
        background: linear-gradient(135deg, rgba(17, 20, 35, 0.92), rgba(15, 17, 30, 0.9));
        border: 1px solid var(--border);
        box-shadow: var(--shadow);
        border-radius: 20px;
        padding: 28px;
        position: relative;
        overflow: hidden;
      }
      .surface::after {
        content: '';
        position: absolute;
        inset: 0;
        background: radial-gradient(circle at 20% 20%, rgba(109, 236, 185, 0.14), transparent 30%),
          radial-gradient(circle at 80% 10%, rgba(130, 180, 255, 0.12), transparent 32%);
        pointer-events: none;
        z-index: 0;
      }
      footer {
        color: var(--muted);
        text-align: center;
        padding: 24px;
        font-size: 14px;
        display: flex;
        gap: 12px;
        justify-content: center;
        flex-wrap: wrap;
        align-items: center;
      }
      .contribute {
        padding: 8px 12px;
        border-radius: 10px;
        border: 1px solid rgba(109, 236, 185, 0.8);
        background: rgba(109, 236, 185, 0.08);
        color: var(--muted);
        font-weight: 700;
      }
      .contribute:visited {
        color: var(--muted);
      }
      .support {
        padding: 8px 12px;
        border-radius: 10px;
        border: 1px solid transparent;
        background: linear-gradient(120deg, var(--accent), var(--accent-2));
        color: #0a0f14 !important;
        font-weight: 700;
        box-shadow: var(--shadow);
        letter-spacing: -0.01em;
        white-space: nowrap;
        transition: transform 0.12s ease, box-shadow 0.12s ease, border 0.2s ease;
      }
      .support:hover {
        transform: translateY(-1px);
        box-shadow: 0 14px 40px rgba(0, 0, 0, 0.35);
        border-color: transparent;
      }
      .legal-links {
        display: inline-flex;
        align-items: center;
        gap: 8px;
      }
      .legal-links a {
        color: var(--muted);
        font-weight: 600;
      }
      .chip {
        display: inline-flex;
        align-items: center;
        gap: 6px;
        padding: 8px 12px;
        border-radius: 999px;
        border: 1px solid var(--border);
        background: rgba(255, 255, 255, 0.04);
        font-size: 13px;
        color: var(--muted);
      }
      @media (max-width: 720px) {
        main {
          padding: 24px 18px 60px;
        }
        .ad-placeholder {
          min-height: 60px;
        }
      }
    </style>
  </head>
  <body>
    <div class="bg-grid" aria-hidden="true"></div>
    <header>
      <nav>
        <div class="exam-bar">
          <div class="nav-left">
            {showBack && <button class="back-btn" type="button" onclick="history.back()">← Back</button>}
            <a class="brand" href="/">
              <span class="pill">AWS</span>
              Exam Prep
            </a>
          </div>
          <div class="exam-links">
            <div class="exam-menu" id="exam-menu">
              <button class="exam-trigger" type="button" aria-haspopup="true" aria-expanded="false">
                Exams ▾
              </button>
              <div class="exam-dropdown" role="menu" aria-label="Exam selector">
                {exams.map((exam) => (
                  <a href={exam.base} role="menuitem" aria-current={exam.id === currentExam ? 'page' : 'false'}>
                    <span>{exam.label}</span>
                    <span class="code">{exam.code}</span>
                  </a>
                ))}
              </div>
            </div>
            {navLinks.map((link) => (
              <a href={link.href}>{link.label}</a>
            ))}
          </div>
        </div>
      </nav>
    </header>
    {showLaunch && (
      <div class="marquee" role="status" aria-live="polite">
        <div class="marquee-track">
          <span>New launch 🚀 Fresh content drops weekly — if you don’t see a topic yet, check back soon.</span>
          <span aria-hidden="true">New launch 🚀 Fresh content drops weekly — if you don’t see a topic yet, check back soon.</span>
          <span aria-hidden="true">New launch 🚀 Fresh content drops weekly — if you don’t see a topic yet, check back soon.</span>
          <span aria-hidden="true">New launch 🚀 Fresh content drops weekly — if you don’t see a topic yet, check back soon.</span>
        </div>
      </div>
    )}
    <main>
      <slot />
      <div class="ad-container" aria-label="Sponsored">
        <div class="ad-placeholder">Ad space — ready for Google AdSense (responsive)</div>
      </div>
    </main>
    <footer>
      <span>Sharpen your cert muscle memory with scenario-first drills.</span>
      <a class="contribute" href={repoUrl} target="_blank" rel="noreferrer">
        Spot an outdated question? Open a PR
      </a>
      <a class="support" href={supportUrl} target="_blank" rel="noreferrer">
        Buy me a coffee
      </a>
      <span class="legal-links">
        <a href="/privacy">Privacy</a>
        <span aria-hidden="true">•</span>
        <a href="/about">About</a>
        <span aria-hidden="true">•</span>
        <a href="/terms">Terms</a>
        <span aria-hidden="true">•</span>
        <a href="/contact">Contact</a>
      </span>
    </footer>
  </body>
</html>
    <script>
      const menu = document.getElementById('exam-menu');
      const trigger = menu?.querySelector('.exam-trigger');
      const dropdown = menu?.querySelector('.exam-dropdown');

      if (trigger && dropdown) {
        trigger.addEventListener('click', () => {
          const isOpen = dropdown.style.display === 'block';
          dropdown.style.display = isOpen ? 'none' : 'block';
          trigger.setAttribute('aria-expanded', String(!isOpen));
        });

        document.addEventListener('click', (e) => {
          if (!menu.contains(e.target)) {
            dropdown.style.display = 'none';
            trigger.setAttribute('aria-expanded', 'false');
          }
        });
      }
    </script>
